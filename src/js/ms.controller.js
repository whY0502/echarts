/**
 * 主控制器
 */
;(function (angular) {
    'use strict';
    /**
     * ms.root.ctrl
     */
    angular.module('ms.root.ctrl', [])
        .controller('MainController', ['$scope',function ($scope) {}]);
})(angular);
